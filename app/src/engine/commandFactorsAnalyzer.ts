/**
 * @deprecated 本文件为旧版前端 TS 引擎，已废弃。
 * 主计算引擎已切换到 Python 后端 API（web/backend/app/api/engine.py）。
 * 当前仅作为 fallback 保留，请勿新增逻辑。
 */
/**
 * 主导因子分析器（P3.4）
 *
 * 命理学价值：
 *   一个真正的命理师拿到一张命盘，不是平铺 14 个维度评分，
 *   而是 5 分钟内抓出"这辈子最重要的 3-5 件事"——这是引擎从"自动初稿"
 *   升级到"有观点的主稿"的关键一步。
 *
 * 设计：
 *   ① 从 6 大来源采集所有候选因子（旺衰/偏枯/调候/大运/格局/keyFindings/关系）
 *   ② 按命理学权重打分（0-100）
 *   ③ Top 3 红（最关键风险）+ Top 3 绿（最关键优势）
 *   ④ 生成"命运主线"一句话定盘
 *   ⑤ 每个因子配 actionableAdvice 翻译为命主可执行的人生决策
 *
 * 加权公式（命理学重要性排序）：
 *   - 极旺/极弱（日主旺衰）：90（命运曲线根本）
 *   - 五行严重偏枯（≥40